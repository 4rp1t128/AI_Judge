# AI Judge - Frontend (React + Vite)


This React frontend connects to the backend you provided (default `http://localhost:3000`). It includes pages to:
- create/get a case
- upload documents for Side A and Side B
- request the initial judge verdict
- send new arguments from Side A / Side B and view the judge's reply
- view case summary and conversation